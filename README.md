# Startup Employee API (Sample)

This repo contains:

- `openapi.yaml` — OpenAPI 3.0 spec for the Employee & Role API.
- `data/employee-database.json` — Sample data for employees and roles in a startup.

You can load `openapi.yaml` into Swagger UI, Redoc, Postman, or any OpenAPI-compatible tool.
